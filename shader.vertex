attribute vec3 position;
attribute vec3 color;
attribute vec3 normal;

varying vec3 sharedColor;

void main() {
  gl_Position = vec4(position, 1.0);
  vec3 x = normal; // placeholder!
  sharedColor = color;
}
