uniform mat3 worldTransformation;

attribute vec2 position;
attribute vec2 textureCoordinate;

void main() {
  mat3 x = worldTransformation;
  vec2 z = textureCoordinate;
  gl_Position = vec4(position, 0, 1);
}
